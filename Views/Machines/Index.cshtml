@{
  Layout = "_Layout";
}
@using Factory.Models;
<div class="container">
  <div class="jumbotron">
    <h3>Machines Index</h3>
  </div>
  @if (Model.Count == 0)
  {
    <p>There are not Machines running at this Factory</p>
  }
  <ol>
    @foreach (Machine machine in Model)
    {
      <li>@Html.ActionLink(machine.Name, "Details", new{id=machine.MachineId})</li>
    }
  </ol>